# Laboratorio Git

Comandos utilizados

git init
git add .
git commit -m "Primer commit"

git branch desarrollo
git checkout desarrollo

git remote add origin https://github.com/Alex050824/laboratorio-git.git
git push -u origin desarrollo

git branch login
git switch login

git add .
git commit -m "Agrega login"
git push origin login

git merge desarrollo
git push
